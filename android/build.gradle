plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.12'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'textlanguage-api'
            version = '1.1.12'

            from components.java

            pom {
                name = 'Text Language'
                description = 'Text Language is a simple tool for detecting the language of a text. It returns the language code.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/textlanguage-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/textlanguage-api.git'
                    url = 'https://github.com/apiverve/textlanguage-api'
                }
            }
        }
    }
}
